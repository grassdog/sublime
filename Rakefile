desc 'Install packages not in package control'
task :install do
	Dir.chdir('~/Library/Application Support/Sublime Text 2/Packages') do
	  system 'git clone https://github.com/grassdog/MarkdownEditing'
	end
end
